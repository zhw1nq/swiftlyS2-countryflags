name: Build & Release

on:
    push:
        branches: [main]
        tags:
            - "v*"
    pull_request:
        branches: [main]

permissions:
    contents: write

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4

            - name: Setup .NET
              uses: actions/setup-dotnet@v4
              with:
                  dotnet-version: 10.0.x

            - name: Restore
              run: dotnet restore

            - name: Build
              run: dotnet build -c Release --no-restore

            - name: Publish
              run: dotnet publish -c Release --no-build

            - name: Upload Artifact
              uses: actions/upload-artifact@v4
              with:
                  name: swiftlyS2-countryflags
                  path: build/publish/swiftlyS2-countryflags/

    release:
        needs: build
        runs-on: ubuntu-latest
        if: startsWith(github.ref, 'refs/tags/v')

        steps:
            - uses: actions/checkout@v4

            - name: Setup .NET
              uses: actions/setup-dotnet@v4
              with:
                  dotnet-version: 10.0.x

            - name: Publish
              run: dotnet publish -c Release

            - name: Create ZIP
              run: |
                  cd build/publish
                  zip -r ../../swiftlyS2-countryflags.zip swiftlyS2-countryflags/

            - name: Create Release
              uses: softprops/action-gh-release@v1
              with:
                  files: |
                      swiftlyS2-countryflags.zip
                  body: |
                      ## Installation
                      1. Extract `swiftlyS2-countryflags.zip` to `addons/swiftlys2/plugins/`
                      2. Configure `config.jsonc` (optional)
                      3. Restart server

                      ## Requirements
                      - SwiftlyS2 v1.0.0+

                      ## Included
                      - GeoLite2-Country.mmdb database (included in `data/` folder)
                  generate_release_notes: true
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
